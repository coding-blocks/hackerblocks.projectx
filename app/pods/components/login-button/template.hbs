{{#if session.isAuthenticated}}
  <button class="button-solid button-orange" onclick={{action 'invalidateSession'}}>LOGOUT</button>
{{else}} 
  <button class="button-solid button-orange" onclick={{action 'logIn'}}>
      LOGIN
  </button>
{{/if}}
