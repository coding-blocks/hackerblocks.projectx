<FullScreenContestView
  @route="contests.contest.attempt"
  @attempt={{contest_attempt}}
  @onTimerEnd={{onTimerEnd}}
  @submitTask={{submitTask}}
  @contest={{contest}}>
  <div class="col-11 offset-1">
    <FullScreenQuizView
      @changeQuestion={{changeQuestion}}
      @quiz_attempt={{quiz_attempt}}
      @index={{q}}
      @quiz={{quiz}}
      @contest={{contest}} />
  </div>
</FullScreenContestView>
